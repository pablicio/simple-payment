<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Payment Simplificado">
    <description>Coding standard for Payment Simplificado</description>

    <!-- Arquivos a serem verificados -->
    <file>app</file>
    <file>config</file>
    <file>database</file>
    <file>routes</file>
    <file>tests</file>

    <!-- Arquivos/pastas a serem ignorados -->
    <exclude-pattern>*/cache/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>
    <exclude-pattern>*.blade.php</exclude-pattern>
    <exclude-pattern>_ide_helper.php</exclude-pattern>

    <!-- Usar o padrão PSR-12 -->
    <rule ref="PSR12"/>

    <!-- Configurações adicionais -->
    <arg name="colors"/>
    <arg value="sp"/>
    <arg name="parallel" value="8"/>
    
    <!-- Nível de erro -->
    <arg value="p"/>

    <!-- Adicionar regras customizadas -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.CodeAnalysis.EmptyStatement"/>
    <rule ref="Generic.CodeAnalysis.UnconditionalIfStatement"/>
    <rule ref="Generic.CodeAnalysis.UselessOverridingMethod"/>
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
    </rule>
    <rule ref="Generic.Metrics.CyclomaticComplexity">
        <properties>
            <property name="complexity" value="10"/>
            <property name="absoluteComplexity" value="15"/>
        </properties>
    </rule>
    <rule ref="Generic.Metrics.NestingLevel">
        <properties>
            <property name="nestingLevel" value="4"/>
            <property name="absoluteNestingLevel" value="6"/>
        </properties>
    </rule>
</ruleset>
